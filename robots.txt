User-agent: *
Allow: /

# タグページはインデックス不要
Disallow: /tags/

# RSS フィード（Hugo が生成する index.xml）はインデックス不要。
# 以前は `Disallow: /*.xml$` としていたが、このパターンは末尾一致のため
# /sitemap.xml にも一致し、下で宣言しているサイトマップ自身を
# ブロックしてしまっていた（Search Console「robots.txt によりブロックされました」）。
# フィードのファイル名で限定する。
Disallow: /index.xml
Disallow: /*/index.xml

Sitemap: https://ai-navi-tools.net/sitemap.xml
